node_modules/
.next/
.env
.env.local
*.log
.vercel/
*.tsbuildinfo

.vercel
.env*

# Google Cloud service-account key files (never commit real credentials)
gen-lang-client-*.json
*service-account*.json
*serviceaccount*.json
*-automation-*.json

# Unrelated project that ended up nested here (2026-08-03) — not part of
# this repo, has its own git history, never touch/commit its contents.
bdc_automation/
